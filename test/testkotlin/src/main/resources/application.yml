spring:
  profiles:
    active: singledatasource
  application:
    name: testkotlin
info:
  version: 1.0.0
  app-name: ${spring.application.name}
  build:
    author: zhangbin
    active: ${spring.profiles.active}