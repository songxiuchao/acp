versions += [
        netty     : "4.1.33.Final",
        httpclient: "4.5.8"
]
dependencies {
    compile project(":core:acp-packet")

    // tcp/udp
    compile "io.netty:netty-all:$versions.netty"

    // http client
    compile "org.apache.httpcomponents:httpclient:$versions.httpclient"
}