group "${group}.core"
versions += [
        netty : "4.1.43.Final",
        okhttp: "3.8.1"
]
dependencies {
    api project(":core:acp-packet")

    // tcp/udp
    api "io.netty:netty-all:$versions.netty"

    // http client
    api "com.squareup.okhttp3:okhttp:$versions.okhttp"
}