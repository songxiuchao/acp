spring:
  profiles:
    active: prod
  application:
    name: server-oauth2
info:
  version: 1.0.0
  app-name: ${spring.application.name}
  build:
    author: zhangbin
    active: ${spring.profiles.active}