spring:
  profiles:
    active: prod
  application:
    name: atomic-hello
info:
  version: 1.0.0
  app-name: ${spring.application.name}
  build:
    author: zhangbin
    active: ${spring.profiles.active}