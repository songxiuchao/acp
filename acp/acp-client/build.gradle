versions += [
        mina       : "2.0.16",
        httpclient : "4.5.6"
]
dependencies {
    compile project(":acp:acp-packet")

    // tcp/udp
    compile "org.apache.mina:mina-core:$versions.mina"

    // http client
    compile "org.apache.httpcomponents:httpclient:$versions.httpclient"
}